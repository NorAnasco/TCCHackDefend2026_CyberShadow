# Enable AndroidX and Jetifier mapping for legacy libraries
android.useAndroidX=true
android.enableJetifier=false

# JVM parameters for optimal build speed and memory usage
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
